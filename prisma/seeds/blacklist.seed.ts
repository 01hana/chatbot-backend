/**
 * blacklist.seed.ts — Phase 1 shell
 *
 * Will be implemented in Phase 1 (T1-003) with initial BlacklistEntry rows.
 */
import { PrismaClient } from '../../src/generated/prisma/client';

export async function seedBlacklist(prisma: PrismaClient): Promise<void> {
  // Implemented in Phase 1 (T1-003)
  void prisma;
}
