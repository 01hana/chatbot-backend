/**
 * intent-templates.seed.ts — Phase 1 shell
 *
 * Will be implemented in Phase 1 (T1-004) with IntentTemplate rows.
 */
import { PrismaClient } from '../../src/generated/prisma/client';

export async function seedIntentTemplates(prisma: PrismaClient): Promise<void> {
  // Implemented in Phase 1 (T1-004)
  void prisma;
}
