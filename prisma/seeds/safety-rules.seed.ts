/**
 * safety-rules.seed.ts — Phase 1 shell
 *
 * Will be implemented in Phase 1 (T1-003) with initial SafetyRule and
 * BlacklistEntry rows.  For Phase 0 this file is intentionally empty.
 */
import { PrismaClient } from '../../src/generated/prisma/client';

export async function seedSafetyRules(prisma: PrismaClient): Promise<void> {
  // Implemented in Phase 1 (T1-003)
  void prisma;
}
